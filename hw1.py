
def sum(list):
    sum = 0
    for idx in range(len(list)):
        sum = sum + list[idx]
    return sum

def mult(list):
    product = 1
    for idx in range(len(list)):
        product = product * list[idx]
    return product
